import React from "react";

// Componente para mostrar el chat seleccionado
const ChatWindow = ({  }) => {
  

  return (
    <div style={{ padding: "20px", border: "1px solid #ddd", marginTop: "10px" }}>
      
    </div>
  );
};

export default ChatWindow;
